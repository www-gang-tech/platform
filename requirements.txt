click>=8.0.0
flask>=3.0.0
pyyaml>=6.0
markdown>=3.4.0
jinja2>=3.1.0
anthropic>=0.18.0
beautifulsoup4>=4.12.0
pillow>=10.0.0
watchdog>=3.0.0
requests>=2.31.0
boto3>=1.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.100.0
# Text layer of ingested PDFs (attachments, Drive). Pure Python; no OCR.
pypdf>=4.0

# Structured local synthesis: response shapes are declared as Pydantic models
# and the generated JSON Schema is handed to Ollama's structured output.
pydantic>=2.0
# Distinguishes genuine quantities from identifiers that merely contain digits.
quantulum3>=0.9

# Test only.
hypothesis>=6.0
